import React from "react";
/**
 * Build a reusable Button component which:
 *
 *  - can accept any arbitrary value and will use it as its own button text value
 *  - can accept a 'theme' property which will change the button's styles (styles from theme.js)
 *  - can accept an 'on click' handler property which can be called from App.js
 *  - can toggle button theme styles when clicking on the button
 *
 */

export default function App() {
  return <button>Hello World</button>;
}
